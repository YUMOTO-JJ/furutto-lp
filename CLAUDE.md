# FURUTTO — Flavor Scroll LP

スクロールでフレーバー（ぶどう→オレンジ→りんご）が切り替わるヒーローLP。
架空のフルーツシェイクブランド。ポートフォリオ用。

ヒーロー＋後続5セクション＋フッターまで実装済み。
残っているのは文言の書き直しと仕上げ（下の「残タスク」）。

## 起動
```
python3 -m http.server 8000    # → http://localhost:8000
```
index.html 単体で完結（ビルド不要）。

## 技術スタック
GSAP 3.12.5 + ScrollTrigger + Lenis（すべてCDN）
イージングは cubic-bezier(0.22, 1, 0.36, 1) 系 / expo.out を基調とする
フォント: Fraunces(900) / Outfit / Noto Sans JP / Shippori Mincho B1

---

## ページ構成

| | セクション | 明るさ | 中身 |
|---|---|---|---|
| — | ヒーロー | 味の色 | 600vh sticky。3味が入れ替わる |
| 02 | `#count` The Count | 暗 | 「果物は書いてある分だけ入っている」。数字がカウントアップ |
| 03 | `#shakes` Our Shakes | 暗 | 3杯を並べて比べる |
| 04 | `#layers` Layers | 暗 | 4層の断面図。レールが上から満ちる |
| 05 | `#promise` Promise | 色が戻る | 三連パネル。3味の空＋素材の寄り |
| 06 | `#stores` Stores | 明 | 締め。4店舗（うち1つは出店予定）＋CTA |
| — | フッター | 暗 | |

**明→暗→色→明** の流れが設計の骨。暗いゾーンが「信じられる根拠」を担当し、
そこを抜けて色が戻ると同じ紫がさっきより濃く見える、という組み立て。

以前あった 07 Franchise は廃止（店舗紹介に統合）。

---

## ヒーローの構造

600vhのトラック内でヒーローをsticky固定し、スクロール量を
「今どのフレーバーか」に変換する。progress 0 / 0.5 / 1 にスナップ。

### レイヤー（奥→手前）
| z | 要素 | 内容 |
|---|------|------|
| 1  | `.l-sky`    | sky_*.webp（背景グラデーション） |
| 5  | `.l-word`   | 巨大タイポ（**縦画面では z9**） |
| 8  | `.l-glass`  | glass_*.webp のみ |
| 9  | `.l-back`   | 奥の果物（液体より奥） |
| 10 | `.l-liquid` | liquid_*.webp（液体の海） |
| 12 | `.l-front`  | 手前の大きい果物2粒（`FRONT_COUNT`） |

すべて `.canvas`（5500/3072のアスペクト比を保つ共通枠）内に
%指定で配置。どの画面幅でも元画像と同じ構図になる。

**重要**: 素材はすべて元画像 5500x3072 の座標系で作られている。
配置データ（JS の `PLACE`）はその座標を%に変換したもの。
素材を差し替える場合は同じ座標系を守ること。

### 触ってはいけない関係

**空・グラス・液体は元が1枚の写真。この3つを別々に拡縮しない。**

- グラスと液体は「グラスの足元に波紋がくぼんでいる」という関係を持つ。
  別スケールにすると液体の上端がただの直線としてグラスを横切る
- 空と液体も同様。縮尺がずれると液体の上端が背景に溶けず、横一直線の段差になる

縦画面でも3つとも同じキャンバスに乗せたままにしてある。
動かしていいのは **果物と文字だけ**。

### 液体の上端をぼかしている理由
空と液体は別画像なので、上端でぷつっと止めるとグラデーションの傾きが折れて
線に見える（りんごが最悪で、境界の上下で傾きが20倍変わっていた）。
`.liquid` に `mask-image: linear-gradient(to bottom, transparent 0, #000 8%)` を
かけて空へ渡している。

### 文字の縦位置
`.word` は `align-items:flex-end` ＋ `padding-bottom` で
**下端を液面から逆算**して置いている（PC `26.5%` / 縦画面 `42.3dvh`）。

液体(z10)は文字より手前なので、文字が液面をまたぐと
**液体の上端が白い文字を定規のように横切って線に見える**。
中央寄せ（旧 `margin-top:-4%`）に戻すとこれが再発する。

---

## 縦画面（`@media (max-width:760px)`）

素材は 1.79:1 の横長。スマホ縦は 0.46:1 なので、高さを埋めると
**中央26%しか映らず果物が1粒も入らない**。トリミングでは解決しない。

- 空・グラス・液体 … PCと同じキャンバスのまま（上記のとおり）
- 文字・果物 … 画面いっぱいの座標系に移す
- 文字は z9（グラスの手前・液体の奥）。液面にディセンダーだけ浸かる
- 果物は `PLACE_SM` で味ごとに **4粒だけ** 並べ直す

### PLACE_SM の作り方（重要）

`fruit_*_N.webp` は**元写真の画面端で見切れていた果物をそのまま切り抜いてある**。
PCは「切れている辺＝画面端」に置いてあるので切れ口が見えない。
縦画面でも同じ原則を守ること。

| 素材の状態 | 置き方 |
|---|---|
| 左が切れている | 左端からはみ出させる（`l` を負に） |
| 右が切れている | 右端からはみ出させる（`l + w > 100`） |
| 上が切れている | ナビと重なるので**縦では使わない** |
| 切れていない | 画面内に自由配置 |

切れている辺は次のコマンドで確認できる（辺の30%以上が不透明なら「切断」）:

```bash
python3 -c "
from PIL import Image; import numpy as np, glob
for p in sorted(glob.glob('assets/fruit_grape_*.webp')):
    a=np.array(Image.open(p).convert('RGBA')); o=a[...,3]>40
    e={'上':o[0].mean(),'下':o[-1].mean(),'左':o[:,0].mean(),'右':o[:,-1].mean()}
    print(p, [f'{k}{v*100:.0f}%' for k,v in e.items() if v>0.3] or 'きれい')"
```

`PLACE_SM` は素材番号 `i` で引く（配列の順番＝大きさ順とは別）。

---

## 切替アニメーション（setFlavor関数）

| 対象 | 動き |
|------|------|
| 背景   | クロスフェード 0.8s |
| 液体   | 下へ抜ける(yPercent 120) / 新は下から満ちる ＋ 横に2.2%流れる |
| グラス | 液体と一緒に沈む / 下から現れる ＋ 着地時に沈み込み(scaleY) |
| 果物   | 旧は落下 / 新は上から時間差(k*0.07s)で降る |
| 文字   | 横軸スライド(xPercent ±130) |
| 全体   | 切替の瞬間だけ blur(6px) → 0 |

### 壊さないための3つの決まり

**1. 全部を1本のタイムライン（`flavorTl`）に入れる。バラの tween を撒かない。**
GSAP の `killTweensOf` は「delay待ちでまだ動き出していない tween」を取りこぼす。
取りこぼした1本が後から発火すると前の味が画面に戻ってくる
（2→0→1 と素早く切り替えるとりんごの液体が残っていた）。
タイムラインごと kill すれば中の予約も消える。**delay ではなく位置引数を使うこと。**

**2. 重力は常に下向き。スクロールの向きで反転させない。**
逆スクロールで液体を上に抜くと、画面全体をなめて残像に見える。
向きで変えていいのは文字の横移動と色だけ。

**3. 入ってくる液体を必ず手前に置く**（`zIndex` 1/2）。
そうしないと、下から満ちる新しい液体が抜けていく古い液体の裏に隠れる。

### 切替判定に不感帯がある
`colorTl` の `onUpdate` で `t1`(0.20/0.30) `t2`(0.70/0.80) を
現在の味によって切り替えている。境界でスクロールが揺れても往復しない。

### 調整ポイント
- `D = 1.25` … 全体の尺の基準
- `FRONT_COUNT = 2` … 文字の手前に出す果物の数
- 果物の落下: `yPercent: -320 - k*40` / 位置 `.28 + k*0.07`
- 液体: `yPercent: 120` / duration `.85` → `1.25`
- グラスの沈み込み: `scaleY 1.014 / 0.993` / 位置 `D*0.62`
- ブラー: `blur(6px)` / `D*0.28` → `D*0.55`

---

## 素材について

すべて手動切り抜き済み。

### ヒーロー用（元画像 5500x3072 の座標系）
- `sky_*.webp`     … 背景グラデーション
- `glass_*.webp`   … グラス（3味でスケール統一）
- `fruit_*_N.webp` … 果物。grape 8粒 / orange 9粒 / apple 9粒
- `liquid_*.webp`  … 液体の海。CSS上 `top:56.315%` に配置
- `thumb_*.webp`   … 商品カード用

### 後続セクション用（透過・独立）
- `fresh_*.webp`   … 果実カット。02 The Count・03 Our Shakes
- `shake_grape.webp` … きれいなグラス。04 Layers
- `promise_*.webp` … 素材の寄り。05 Promise（3味そろい）

### 既知の不具合
- **`sky_orange` / `sky_apple` に果物の消し残り**（画像の左寄り x≈33%、
  オレンジ2個・りんご1個）。Promise では `object-position:100% 50%` で
  右端の無地を使って回避している。作り直す機会があれば直したい
- `fruit_*_N.webp` は端が切れている（上記「PLACE_SM の作り方」）

### 新しい素材を入れるとき
1. **白背景のままアップスケール → そのあと切り抜き**の順にする。
   切り抜き済みを拡大すると透明の縁に沿って色が作られ、マゼンタの縁が出る
2. 透明余白をトリミングしてから WebP 化（%指定が素直になる）
3. 縁のフリンジは下の方法で実測する。既存の合格素材は「平均22〜25」

---

## 検証のやり方（ハマりどころ）

**macOSのChromeはウィンドウ幅を500px未満にできない。**
`--window-size=390,844` を指定しても**500pxでレイアウトして390pxに切り取る**。
モバイル検証は必ず「iframeを390px幅で作る」ハーネス経由で行うこと。

```html
<!-- iframe を実寸で作って、その中を測る／撮る -->
<iframe id="frame" src="/index.html" style="width:390px;height:844px;border:0"></iframe>
```

**ブラウザペインもヘッドレスの仮想時間も rAF が進まない**ので、
GSAP のアニメーションは実時間では測れない。
`gsap.updateRoot(t)` でルート時計を手で進めると、delay も kill も
正しく効いた状態で終端まで再生できる（`t.progress(1)` は
kill済みや `gsap.set` まで実行してしまうので嘘が出る）。

```js
const adv = sec => { const t0 = w.gsap.globalTimeline.time();
  for (let x = 0.02; x <= sec; x += 0.02) w.gsap.updateRoot(t0 + x); };
```

フォント幅を測るときは `await d.fonts.ready` を待つ（Fraunces は
フォールバック比で2割違う）。

---

## 作業ルール

- 実装してから報告。質問は最小限に
- デザイン判断（見た目・動きの好み）はユーザーに確認する
- 技術的な実装方法は任せてよい
- 説明はデザイナー向けの言葉で（コードの詳細より「何がどう変わるか」）
- 3回試して解決しない場合はエスカレーション

## 残タスク

1. **文言のラフ化** … 全体的に硬いのでまとめて書き直す
2. **仕上げ** … OGP、meta description、favicon
3. **prefers-reduced-motion** … 後続セクションは対応済み（`REDUCE` で分岐）。
   ヒーローのスクロール演出は未対応
4. **パフォーマンス** … 画像のプリロード戦略（現状は全フレーバー同時ロード。
   assets 合計 1.1MB）
5. **CTA** … `href="#"` のダミーが複数残っている
